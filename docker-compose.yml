version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: devpath
      POSTGRES_USER: devpath  
      POSTGRES_PASSWORD: devpath123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  volumes:
    postgres_data:
